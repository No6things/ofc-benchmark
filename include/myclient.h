#ifndef MYCLIENT_H
#define MYCLIENT_H

int clientSide(const char *nodeMasterHostname);

/*struct node
{
    char* data;
    struct node *next;
};

struct node *front = NULL;
struct node *rear = NULL;
*/
#endif
